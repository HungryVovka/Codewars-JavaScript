// -----------------------------------------------------------
// Return the type of the sum of the two arguments
// -----------------------------------------------------------

function typeOfSum(a, b) {
  var answer = a + b;
  return typeof(answer);
}